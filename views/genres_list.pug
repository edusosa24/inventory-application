extends layout

block content
  h2= title

  div.crud-functions
    a(class="toForm" href="/catalog/genre/create") ADD GENRE

  ul(class="all-list")
    each gen in genres_list
      li
        p
          a(href=gen.url) #{gen.name}

    else
      li There are no genres.

